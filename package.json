{
  "name": "local-sketch-deck",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:site": "vite --config vite.site.config.ts",
    "build:app": "vite build --outDir dist/app",
    "build:site": "vite build --config vite.site.config.ts --outDir dist/site",
    "build": "npm run build:app && npm run build:site",
    "test": "tsx --test test/**/*.test.ts",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.0",
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0"
  }
}
